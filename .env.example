# Server
PORT=5000

# MySQL connection
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=clinicare
DB_CONNECTION_LIMIT=10

# Set to true to let Sequelize create/alter tables on startup
DB_SYNC=true

# JWT
# IMPORTANT: change this to a strong random value in your real .env
JWT_SECRET=change-me-in-.env
JWT_EXPIRES_IN=1d
